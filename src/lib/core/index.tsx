import React from 'react';

const Core: React.FC = () => {
  React.useEffect(() => {
    // Core initialization
    console.log('Core component initialized');
  }, []);

  return null;
};

export default Core;